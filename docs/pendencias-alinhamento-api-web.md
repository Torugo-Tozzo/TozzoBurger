# Alinhamento do mobile com API e web

**Criado em:** 2026-09-25. Este é o registro contínuo das diferenças de produto e contrato. Nenhuma linha significa que a funcionalidade já foi implementada no mobile. A implementação atual continua concentrada na API e na web. Veja o [plano da mudança de negócio](../../api-tozzo.uk/proximas-etapas/002-plataforma-multinicho/README.md).

## Como manter este arquivo

Em **toda entrega na API ou web**, antes de encerrar:

1. Revisar se surgiram campos, valores de enum, endpoints, telas ou regras que afetam o mobile, mesmo quando o escopo da entrega é apenas API/web.
2. Adicionar ou atualizar uma linha abaixo com data, origem (arquivo/commit), impacto no app instalado, compatibilidade da API, ação mobile e verificação pendente. Escrever `sem impacto mobile` quando a revisão concluir isso, com motivo, para não depender da memória.
3. Não marcar como concluído por existir no backend ou na web. Fechar a linha somente depois de código mobile, migration local quando necessária, teste automatizado e homologação no Android; registrar commit/build e resultado.
4. Para contratos novos, conferir `/openapi.json` e testes HTTP reais. A documentação gerada ainda tem respostas `t.Any()`; não inferir payload apenas pela UI Scalar.

Estados: **Confirmado** = diferença encontrada no código; **Verificar** = há mudança recente, mas o efeito exato no app precisa de teste; **Planejado** = mudança futura no plano da API/web; **Concluído** = entregue e validado no dispositivo.

## Diferenças atuais

| ID | Estado | Origem API/web | Evidência no mobile e ação necessária | Aceite mobile |
| --- | --- | --- | --- | --- |
| M01 | Confirmado, prioridade alta | API `OrderItemStatus.READY`, `modules/kitchen/*`, sync `order_items.status`; web `KitchenPage.tsx` (2026-09-25) | `database/watermelon/models/OrderItem.ts`, `database/types/Order.ts`, `database/useOrderDatabase.ts` e `app/modais/pedidoModal.tsx` ainda aceitam `REQUESTED`, `IN_PREPARATION`, `DELIVERED`, sem `READY`. Novos itens da API entram em `IN_PREPARATION`; `REQUESTED` de push legado é aceito e normalizado, sem regredir `READY`/`DELIVERED`. Atualizar o modelo e migração WatermelonDB, usar apenas Em preparo/Pronto/Entregue, retirar Começar preparo, preservar FIFO e testar sync bidirecional. | Pull real com item `READY`; criar pedido local já Em preparo; upgrade de banco com itens antigos; editar/sincronizar sem erro nem regressão de status; cozinha e papel `COOK` validados. |
| M02 | Confirmado | API `Product.productTypeId` opcional; web `ProductsPage.tsx` usa opção “sem tipo” (mudança recente) | `app/modais/produtoModal.tsx` ainda bloqueia salvar sem `productTypeId`. Remover obrigatoriedade no formulário, enviar `null`/omitir conforme contrato, exibir “Não informado”, conferir filtros, relatórios e sync de produtos com tipo nulo. | Criar, editar, puxar e vender item sem tipo; app não cria tipo artificial. |
| M03 | Confirmado/Verificar | Método de pagamento em `Sale.paymentMethod`; web mostra em vendas/recibos (2026-09-22) | Não há uso de `paymentMethod` em `app/`, `services/` ou `database/`. Planejar captura/exibição conforme o fluxo mobile e testar que pushes antigos não limpam o campo do servidor. Verificar antes a regra para venda offline e métodos aceitos pela API. | Venda mobile com método correto quando o contrato offline estiver definido; venda antiga permanece válida; recibo/histórico consistentes. |
| M04 | Confirmado/Verificar | `modules/delivery/*`, despacho e motorista; web `DeliveriesPage.tsx` (2026-09-23/24) | Não há fluxo próprio de entregas em `app/(tabs)/`. `database/watermelon/schema.ts` não contém campos de despacho da ordem. Mapear o contrato, decidir suporte offline e incluir tela/ações somente depois da API estável; verificar se pulls antigos já ignoram os campos sem sobrescrevê-los. | Pedido para entrega e despacho visualizados corretamente; atribuição, início e conclusão testados; app antigo não regride pedido. |
| M05 | Confirmado | `modules/calendar/*`, horário de funcionamento e escala; web `SchedulePage.tsx`/`BusinessHoursPanel.tsx` (2026-09-24) | Não há tela de calendário/escala no mobile; `database/watermelon/schema.ts` não tem essas entidades. Definir se primeira versão mobile será leitura online ou terá armazenamento offline e migration; manter edição separada de cadastro de funcionário. | Semana/mês e turnos, expediente, fuso e alterações concorrentes validados no Android conforme escopo decidido. |
| M06 | Verificar | Papel `COOK` criado na API/web | `services/legacyWire.ts` não mapeia `COOK`; fallback é `EMPLOYEE`. Revisar autenticação, navegação e permissões do app antes de habilitar esse papel no mobile. | Usuário `COOK` vê apenas telas autorizadas; chamadas proibidas falham de forma clara. |
| M07 | Verificar | Tipos de produto agora pertencem a um estabelecimento (`2026-09-21`) | `product_types` existe no WatermelonDB e o onboarding mobile ainda usa a categoria antiga. Verificar pull/push com dois estabelecimentos, IDs UUID e troca de conta; não presumir que os testes existentes cobrem a migration recente da API. | Tipos e produtos não vazam entre estabelecimentos; troca de conta preserva isolamento. |
| M08 | Em implementação na API/web | Perfis `FOOD`, `STORE`, `SERVICES`; `GET/PATCH /establishments/preferences` com `profiles`, `visibleModules`, `revision`/`expectedRevision`; coluna legada `category` permanece. `SALES`, `PRODUCTS`, `EMPLOYEES`, `DEVICES`, `REPORTS`, `SETTINGS` são padrão conforme papel; códigos antigos `MENU_ITEMS`/`STORE_PRODUCTS` normalizados para `PRODUCTS`. Desativar FOOD limpa `ORDERS`/`KITCHEN`/`DELIVERIES`; desativar SERVICES limpa `SERVICES`/`ESTIMATES`; `SCHEDULE` é geral. | `app/onboarding.tsx` e `services/categoryOnboarding.ts` ainda escolhem **uma** categoria de alimentação. Apps instalados não chamam o endpoint novo e continuam recebendo o contrato legado. Futuramente adicionar perfis múltiplos, edição em Configurações com módulos agrupados sob cada perfil e menu por módulos, mantendo a categoria antiga até migração segura. Perfil de serviço deve aparecer como “Prestador de serviços”; módulo continua “Serviços”. Loja terá venda direta; Serviços e Orçamentos dependem da Etapa 3. | Alimentação + Prestador de serviços aparecem juntos após implantação dos módulos; mudar perfil não apaga dados; módulos padrão ficam disponíveis conforme papel. |
| M09 | Planejado | Catálogo único `TB_PRODUCTS` para alimentação, loja e peças; `OrderItem.status` só em pedidos; venda direta da Loja sem `Order`; ordem de serviço com orçamento/prazo e linhas de produtos e mão de obra | `database/watermelon/schema.ts` já tem `products`; `order_items` e `sale_items` usam `product_id`. Não migrar produtos para tabela de cardápio. Planejar suporte à venda direta, novas entidades de serviço e eventual migration WatermelonDB; manter sync legado de produtos. Não resetar SQLite para contornar migração. | IDs/histórico de produtos preservados; Loja vende sem pedido; mobile antigo continua sincronizando alimentação; serviços fecham em venda sem cozinha. |
| M10 | Em andamento | OpenAPI/Scalar na API (2026-09-25) | `/api-docs` já usa Scalar e `/openapi.json` lista as rotas; `/swagger.json` foi removido sem consumidores. Cozinha, entregas e calendário receberam resumo, tag e segurança. Ainda há 63 respostas 200 com schema vazio. A Etapa 4 ensaiará `fromTypes` e DTOs HTTP; usar testes de contrato como fonte até lá. | Endpoints que o mobile consumir têm requests, responses, erros e segurança documentados e comparados a HTTP real. |
| M11 | Planejado | Preferências aceitam `STORE_INVENTORY` em Loja e `INGREDIENT_INVENTORY` em Alimentação; a web mostra ambos como módulos futuros, desmarcados por padrão (2026-09-25) | Nenhum saldo, movimento, ingrediente, receita ou baixa automática existe ainda na API ou no mobile. Ao implementar, controle de estoque será opt-in por produto pronto ou ingrediente. Lanche/cardápio não recebe estoque próprio automaticamente; receita/ficha técnica opcional ligará venda ao consumo de ingredientes. Planejar schema e migração WatermelonDB somente quando o contrato da API estiver definido. | Ativar/desativar estoque por item sem afetar produtos existentes, vendas sem estoque e sync legado; validar saldo, unidade, ajuste e consumo de receita em homologação. |

O app mobile **não está inteiramente parado**: já usa GoTrue, possui onboarding de categoria única, catálogo de alimentação, pedidos, vendas e sync offline. Os itens acima registram diferenças comprovadas ou que exigem ensaio, sem presumir que toda mudança recente esteja ausente.

## Ordem sugerida para a retomada mobile

1. **Compatibilidade imediata:** M01, M02, M03 e M07, priorizando o app instalado e a preservação de dados no sync.
2. **Fluxos recentes:** M04, M05 e M06 depois de confirmar contratos e definir o que funciona offline.
3. **Novo modelo de negócio:** M08, M09 e M11 após as etapas correspondentes da API/web e publicação de um contrato de sync compatível.
4. **Homologação:** executar Jest, build debug, upgrade sobre banco local já populado, sync com a API de homologação em estabelecimento de teste isolado, troca de conta, perda/retorno de rede e testes de interface em dispositivo/emulador. Impressão Bluetooth requer hardware para aceite físico. Registrar versão do APK, ambiente e resultado aqui.

## Registro de entregas futuras

| Data/commit API ou web | Mudança e contrato | Impacto mobile / estado | Código mobile | Teste e homologação |
| --- | --- | --- | --- | --- |
| 2026-09-25, planejamento | Perfis múltiplos, catálogo único de produtos, Loja e Serviços; plano em `api-tozzo.uk/proximas-etapas/002-plataforma-multinicho/` | M08/M09; o endpoint de preferências foi implementado na primeira etapa | Nenhum | Venda direta e serviços aguardam API/web |
| 2026-09-25, API `90dae1c` | `@elysia/openapi` + Scalar em `/api-docs`; JSON em `/openapi.json`, 65 operações; `/swagger.json` removido | M10, em andamento; app instalado não consome a documentação | Nenhum | 337 testes API, typecheck, Prisma validate e página no Chromium; schemas de resposta ainda pendentes |
| 2026-09-25, workspace API/web | Preferências multinicho e menu configurável: novo contrato autenticado `GET/PATCH /establishments/preferences` com revisão otimista; backfill `FOOD` para estabelecimentos existentes; onboarding web múltiplo e edição em Configurações | M08, primeira etapa implementada na API/web. Mobile antigo não grava as novas colunas; `category`, `/produtos` e sync legado permanecem. Ainda falta ensaiar app instalado contra API migrada. | Nenhum | API 347 testes + migration PostgreSQL descartável + typecheck/Prisma; web 251 testes + i18n/build; Chromium 1 passou, 3 autenticados pulados sem credenciais. Homologação Android pendente. |
| 2026-09-25, correção API/web local | Seis módulos padrão, `PRODUCTS` único e normalização dos códigos de menu salvos; Loja deixa de sugerir Entregas até haver entrega de venda direta. `TB_PRODUCTS` não muda; o preparo já está em `OrderItem.status`. | M08/M09: mobile não precisa de migration de catálogo. Ao adotar o menu novo, mapear `PRODUCTS` e respeitar papel. Não publicar entidades futuras de serviço no sync legado. | Nenhum | API 347 testes, migration PostgreSQL descartável, typecheck/Prisma; web 251 testes, i18n/build. Chromium: 1 público passou; autenticados pendentes porque a conta E2E da homologação não autentica no GoTrue local. Migration aplicada só no PostgreSQL local; homologação Android pendente. |
| 2026-09-25, simplificação API/web local | Novo `OrderItem` começa em `IN_PREPARATION`; `REQUESTED` legado normalizado na API/sync; migration SQL converte registros antigos e atualiza a revisão do pedido; cozinha com Em preparo/Pronto. Sync passa a revisar o pedido uma vez ao fim do lote de itens. Preferências agrupadas por perfil, sem prévia do menu; desligar um perfil limpa seus módulos específicos, mantendo Horários e os seis padrão. | M01/M08: app atual segue enviando `REQUESTED` até migração mobile. Implementar `READY`, novo default e menus agrupados; testar upgrade WatermelonDB sem perder dados e push de vários itens do mesmo pedido. | Nenhum | Migration e limpeza de preferências validadas em PostgreSQL descartável e aplicadas no PostgreSQL local; API 358 testes, typecheck/Prisma; web 253 testes, i18n/build; Chromium 1 público passou, 3 autenticados pulados sem credenciais; homologação Android pendente. |
| 2026-09-25, módulos futuros de estoque | API/web reconhecem `STORE_INVENTORY` e `INGREDIENT_INVENTORY` nas preferências, sem página nem schema de saldo; os dois ficam desmarcados por padrão e têm aviso de atualização futura. | M11: app instalado ignora esses códigos; futura implementação mobile só após API de estoque e ingredientes, sem transformar todos os produtos em itens estocáveis. | Nenhum | API 360 testes, typecheck; web 255 testes, i18n/build; Chromium 1 público passou e 3 autenticados pulados sem credenciais. Contrato de estoque e homologação Android ainda futuros. |
| 2026-09-25, acesso local à API | O Vite encaminha `/api/*` para a API local no desenvolvimento; não há alteração de endpoint, payload ou URL da API publicada. Monitoramento prolongado de listagens e SSE ficou em `proximas-etapas/002-plataforma-multinicho/TODO-monitoramento-listagens.md` na API para 2026-09-26. | Nenhum ajuste no app mobile; ele segue usando a URL da API diretamente. | Nenhum | `/api/health`, perfil, Produtos, Pedidos, Vendas e SSE responderam via Vite; build, i18n e 255 testes web passaram. O ensaio de várias sessões Chromium ociosas ainda está pendente. |

Adicionar uma linha por entrega; atualizar IDs acima quando a mesma entrega avançar. Em cada fechamento, anexar referência ao contrato OpenAPI ou teste HTTP, migration WatermelonDB se houver, commit mobile e evidência de homologação.
