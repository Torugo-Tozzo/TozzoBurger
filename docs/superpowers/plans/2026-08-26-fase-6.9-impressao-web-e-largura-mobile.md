# Fase 6.9 — Impressão web (pedido/venda) + largura de papel configurável — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dashboard web ganha impressão de pedido/venda (botão já reservado na coluna "Ações"), e tanto web quanto mobile ganham uma preferência de largura de papel (44/58/76/80/110mm + A4 só no web) persistida por dispositivo, generalizando a formatação que hoje está hardcoded em 48 colunas no mobile.

**Architecture:** Web: `window.print()` contra um container escondido montado via portal (`react-dom/client` `createRoot`), CSS `@media print` esconde o resto da página, `@page` é injetado dinamicamente por preset de largura. Mobile: a função de formatação ESC/POS existente (`formatarVendaParaImpressao`) passa a ler a largura persistida (`AsyncStorage`) e usar uma tabela largura→colunas em vez do `48` fixo — continua imprimindo só via Bluetooth, sem mudança de mecanismo.

**Tech Stack:** Front: React 18 + TypeScript + Vite, `react-dom/client`, Tailwind (CSS puro pro print, fora do Tailwind). Mobile: React Native (Expo SDK 52) + TypeScript, `@react-native-async-storage/async-storage`, `@react-native-picker/picker`.

**Spec:** `C:/RN/TozzoBurger/docs/superpowers/specs/2026-08-26-fase-6.9-impressao-web-design.md`

## Global Constraints

- Branch front: `feat/fase-6.9-print-paper-width`, a partir de `dev`.
- Branch mobile: `feat/fase-6.9-print-paper-width`, a partir de `dev`.
- Nenhuma mudança na api (`api-tozzo.uk`) — pedido/venda já chegam completos nas tabelas do dashboard, sem endpoint novo.
- Toda chave i18n nova precisa existir, com o mesmo texto-molde (`{{id}}` etc.), nos 6 locales de cada repo (`en`, `pt-BR`, `es`, `fr`, `zh`, `hi`) — o checker (`bun run i18n:check` no front, teste de checker no mobile) falha se faltar ou sobrar chave.
- Front: comando de teste é `bun test`; `bunx tsc --noEmit` limpo é requisito de toda task que mexe em `.tsx`/`.ts`; `bun run i18n:check` limpo é requisito de toda task que mexe em locale.
- Mobile: comando de teste é `npx jest --watchAll=false --runInBand`; `npx tsc --noEmit` limpo é requisito de toda task que mexe em `.tsx`/`.ts`.
- Larguras de papel térmico e colunas ESC/POS correspondentes (Font A, 203dpi, aproximado — não há impressora física de 44/76/110mm pra calibrar, ajuste fino futuro é 1 número):

  | Preset | Colunas (mobile) |
  |---|---|
  | `44mm` | 24 |
  | `58mm` | 32 |
  | `76mm` | 42 |
  | `80mm` | 48 (comportamento atual, hardcoded) |
  | `110mm` | 64 |
  | `a4` | n/a — só existe no web |

- Nomes de preset são os valores literais da tabela acima (`'44mm' | '58mm' | '76mm' | '80mm' | '110mm'` no mobile, mais `'a4'` no web) — usados como chave de storage e como label visível (não são traduzidos: "58mm" se lê igual em qualquer idioma).

---

## Task 1: Preparar branches (front + mobile)

**Files:** nenhum — só setup de branch nos 2 repos.

**Interfaces:** N/A.

- [ ] **Step 1: Front — criar a branch a partir de `dev`**

Rodar em `C:/RN/front/front-tozzo.uk`:

```bash
git checkout dev
git pull origin dev
git checkout -b feat/fase-6.9-print-paper-width
```

- [ ] **Step 2: Mobile — criar a branch a partir de `dev`**

Rodar em `C:/RN/TozzoBurger`:

```bash
git checkout dev
git pull origin dev
git checkout -b feat/fase-6.9-print-paper-width
```

---

## Task 2 (front): `printPreferences.ts` — preferência de largura no localStorage

**Files:**
- Create: `src/lib/printPreferences.ts`
- Test: `src/lib/printPreferences.test.ts`

**Interfaces:**
- Consumes: nada (módulo raiz).
- Produces: `PAPER_WIDTH_PRESETS` (array readonly), `PaperWidthPreset` (union type: `'44mm' | '58mm' | '76mm' | '80mm' | '110mm' | 'a4'`), `DEFAULT_PAPER_WIDTH` (`'80mm'`), `getStoredPaperWidth(storage?)`, `persistPaperWidth(value, storage?)`. Tasks 3 e 6 consomem esses exports.

- [ ] **Step 1: Escrever o teste (mesmo padrão de `src/i18n/locale.test.ts`)**

```typescript
// src/lib/printPreferences.test.ts
import { describe, expect, test } from 'bun:test'

type MemoryStorage = {
  getItem: (key: string) => string | null
  setItem: (key: string, value: string) => void
}

function makeStorage(initial: Record<string, string> = {}): MemoryStorage {
  const values = new Map(Object.entries(initial))
  return {
    getItem: (key) => values.get(key) ?? null,
    setItem: (key, value) => values.set(key, value),
  }
}

describe('print preferences', () => {
  test('falls back to 80mm when nothing is stored', async () => {
    const prefs = await import('./printPreferences')
    expect(prefs.getStoredPaperWidth(makeStorage())).toBe('80mm')
  })

  test('reads a valid stored preset', async () => {
    const prefs = await import('./printPreferences')
    const storage = makeStorage({ 'tozzo.printerWidth': '58mm' })
    expect(prefs.getStoredPaperWidth(storage)).toBe('58mm')
  })

  test('falls back to 80mm for a corrupted/unknown value', async () => {
    const prefs = await import('./printPreferences')
    const storage = makeStorage({ 'tozzo.printerWidth': 'not-a-preset' })
    expect(prefs.getStoredPaperWidth(storage)).toBe('80mm')
  })

  test('persists a valid preset and returns it', async () => {
    const prefs = await import('./printPreferences')
    const storage = makeStorage()
    expect(prefs.persistPaperWidth('110mm', storage)).toBe('110mm')
    expect(storage.getItem('tozzo.printerWidth')).toBe('110mm')
  })

  test('persisting an invalid value falls back to 80mm', async () => {
    const prefs = await import('./printPreferences')
    const storage = makeStorage()
    expect(prefs.persistPaperWidth('bogus', storage)).toBe('80mm')
    expect(storage.getItem('tozzo.printerWidth')).toBe('80mm')
  })

  test('PAPER_WIDTH_PRESETS has exactly the six supported presets', async () => {
    const prefs = await import('./printPreferences')
    expect(prefs.PAPER_WIDTH_PRESETS).toEqual(['44mm', '58mm', '76mm', '80mm', '110mm', 'a4'])
  })
})
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

```bash
bun test src/lib/printPreferences.test.ts
```

Esperado: falha, `printPreferences.ts` não existe ainda.

- [ ] **Step 3: Implementar**

```typescript
// src/lib/printPreferences.ts
export const PAPER_WIDTH_PRESETS = ['44mm', '58mm', '76mm', '80mm', '110mm', 'a4'] as const

export type PaperWidthPreset = (typeof PAPER_WIDTH_PRESETS)[number]

export const DEFAULT_PAPER_WIDTH: PaperWidthPreset = '80mm'
export const PAPER_WIDTH_STORAGE_KEY = 'tozzo.printerWidth'

export type PrintPreferenceStorage = Pick<Storage, 'getItem' | 'setItem'>

function isValidPreset(value: unknown): value is PaperWidthPreset {
  return typeof value === 'string' && (PAPER_WIDTH_PRESETS as readonly string[]).includes(value)
}

function defaultStorage(): PrintPreferenceStorage | undefined {
  if (typeof window === 'undefined') return undefined
  try {
    return window.localStorage
  } catch {
    return undefined
  }
}

export function getStoredPaperWidth(
  storage: PrintPreferenceStorage | undefined = defaultStorage(),
): PaperWidthPreset {
  try {
    const stored = storage?.getItem(PAPER_WIDTH_STORAGE_KEY)
    return isValidPreset(stored) ? stored : DEFAULT_PAPER_WIDTH
  } catch {
    return DEFAULT_PAPER_WIDTH
  }
}

export function persistPaperWidth(
  value: unknown,
  storage: PrintPreferenceStorage | undefined = defaultStorage(),
): PaperWidthPreset {
  const preset = isValidPreset(value) ? value : DEFAULT_PAPER_WIDTH
  try {
    storage?.setItem(PAPER_WIDTH_STORAGE_KEY, preset)
  } catch {
    // Preference is best-effort; an unavailable storage must not break the UI.
  }
  return preset
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

```bash
bun test src/lib/printPreferences.test.ts
```

Esperado: 6/6 pass.

- [ ] **Step 5: `tsc` e commit**

```bash
bunx tsc --noEmit
git add src/lib/printPreferences.ts src/lib/printPreferences.test.ts
git commit -m "feat(front): paper-width preference storage (localStorage)"
```

---

## Task 3 (front): `ReceiptPrintView` + CSS de impressão + `printReceipt()`

**Files:**
- Create: `src/components/receipt/ReceiptPrintView.tsx`
- Create: `src/components/receipt/printReceipt.tsx`
- Create: `src/components/receipt/printReceipt.test.tsx`
- Modify: `src/index.css` (adiciona as regras `@media print` no final do arquivo)

**Interfaces:**
- Consumes: `PaperWidthPreset`, `getStoredPaperWidth` (Task 2); `formatCurrencyBRL` de `@/i18n/format` (já existe).
- Produces: `ReceiptLineItem` (`{ name: string, quantity: number, unitPrice: number }`), `ReceiptData` (`{ title: string, customerName: string, dateLabel: string, items: ReceiptLineItem[], total: number, locale?: string }`), `printReceipt(data: ReceiptData): void`. Tasks 4 e 5 consomem `printReceipt` e `ReceiptData`.

- [ ] **Step 1: Componente do recibo**

```tsx
// src/components/receipt/ReceiptPrintView.tsx
import { formatCurrencyBRL } from '@/i18n/format'
import type { PaperWidthPreset } from '@/lib/printPreferences'

export interface ReceiptLineItem {
  name: string
  quantity: number
  unitPrice: number
}

export interface ReceiptData {
  title: string
  customerName: string
  dateLabel: string
  items: ReceiptLineItem[]
  total: number
  locale?: string
}

interface ReceiptPrintViewProps {
  data: ReceiptData
  width: PaperWidthPreset
}

export function ReceiptPrintView({ data, width }: ReceiptPrintViewProps) {
  return (
    <div className={`receipt receipt--${width}`}>
      <div className="receipt__brand">TOZZO BURGER</div>
      <div className="receipt__title">{data.title}</div>
      <div className="receipt__meta">{data.customerName}</div>
      <div className="receipt__meta">{data.dateLabel}</div>
      <hr className="receipt__rule" />
      {data.items.map((item, index) => (
        <div className="receipt__item" key={index}>
          <span className="receipt__item-name">
            {item.quantity}x {item.name}
          </span>
          <span className="receipt__item-price">
            {formatCurrencyBRL(item.quantity * item.unitPrice, data.locale)}
          </span>
        </div>
      ))}
      <hr className="receipt__rule" />
      <div className="receipt__total">
        <span>Total</span>
        <span>{formatCurrencyBRL(data.total, data.locale)}</span>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: CSS de impressão — adicionar ao final de `src/index.css`**

```css
/* Fase 6.9 — impressão de recibo (pedido/venda) */
#receipt-print-root {
  display: none;
}

@media print {
  body > *:not(#receipt-print-root) {
    display: none !important;
  }
  #receipt-print-root {
    display: block !important;
  }
}

.receipt {
  font-family: 'Courier New', Courier, monospace;
  font-size: 12px;
  color: #000;
}

.receipt--44mm { width: 44mm; }
.receipt--58mm { width: 58mm; }
.receipt--76mm { width: 76mm; }
.receipt--80mm { width: 80mm; }
.receipt--110mm { width: 110mm; }
.receipt--a4 { width: 190mm; }

.receipt__brand {
  text-align: center;
  font-weight: bold;
  font-size: 14px;
}

.receipt__title,
.receipt__meta {
  text-align: center;
}

.receipt__rule {
  border: none;
  border-top: 1px dashed #000;
  margin: 6px 0;
}

.receipt__item {
  display: flex;
  justify-content: space-between;
  gap: 4px;
}

.receipt__item-name {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.receipt__total {
  display: flex;
  justify-content: space-between;
  font-weight: bold;
  margin-top: 8px;
}
```

- [ ] **Step 3: Escrever o teste de `printReceipt` (antes de implementar, vai falhar)**

```tsx
// src/components/receipt/printReceipt.test.tsx
import { afterEach, describe, expect, mock, test } from 'bun:test'
import { persistPaperWidth } from '@/lib/printPreferences'
import { printReceipt } from './printReceipt'

describe('printReceipt', () => {
  afterEach(() => {
    document.getElementById('receipt-print-root')?.remove()
    document.body.style.pointerEvents = ''
  })

  test('mounts the receipt root, calls window.print, and cleans up on afterprint', async () => {
    persistPaperWidth('58mm')
    const printSpy = mock(() => {})
    window.print = printSpy as unknown as typeof window.print

    printReceipt({
      title: 'Sale #1',
      customerName: 'Jane Doe',
      dateLabel: '2026-08-26 10:00',
      items: [{ name: 'Burger', quantity: 2, unitPrice: 10 }],
      total: 20,
    })

    await new Promise((resolve) => requestAnimationFrame(resolve))

    const root = document.getElementById('receipt-print-root')
    expect(root).not.toBeNull()
    expect(root?.className).toContain('receipt--58mm')
    expect(printSpy).toHaveBeenCalledTimes(1)

    window.dispatchEvent(new Event('afterprint'))
    expect(document.getElementById('receipt-print-root')).toBeNull()
  })
})
```

- [ ] **Step 4: Rodar o teste e confirmar que falha**

```bash
bun test src/components/receipt/printReceipt.test.tsx
```

Esperado: falha, `printReceipt.tsx` não existe.

- [ ] **Step 5: Implementar `printReceipt`**

```tsx
// src/components/receipt/printReceipt.tsx
import { createRoot } from 'react-dom/client'
import { getStoredPaperWidth, type PaperWidthPreset } from '@/lib/printPreferences'
import { ReceiptPrintView, type ReceiptData } from './ReceiptPrintView'

const PAGE_SIZE: Record<PaperWidthPreset, string> = {
  '44mm': '44mm auto',
  '58mm': '58mm auto',
  '76mm': '76mm auto',
  '80mm': '80mm auto',
  '110mm': '110mm auto',
  a4: 'A4',
}

export function printReceipt(data: ReceiptData): void {
  const width = getStoredPaperWidth()

  const container = document.createElement('div')
  container.id = 'receipt-print-root'
  document.body.appendChild(container)

  const pageStyle = document.createElement('style')
  pageStyle.textContent = `@page { size: ${PAGE_SIZE[width]}; margin: 0; }`
  document.head.appendChild(pageStyle)

  const root = createRoot(container)
  root.render(<ReceiptPrintView data={data} width={width} />)

  let cleaned = false
  const cleanup = () => {
    if (cleaned) return
    cleaned = true
    window.removeEventListener('afterprint', cleanup)
    root.unmount()
    container.remove()
    pageStyle.remove()
  }

  window.addEventListener('afterprint', cleanup)
  // Safety net for browsers that never fire afterprint (rare, but seen on
  // some print-to-PDF flows) — do not leave the hidden root mounted forever.
  setTimeout(cleanup, 60_000)

  requestAnimationFrame(() => window.print())
}

export type { ReceiptData, ReceiptLineItem } from './ReceiptPrintView'
```

- [ ] **Step 6: Rodar o teste e confirmar que passa**

```bash
bun test src/components/receipt/printReceipt.test.tsx
```

Esperado: PASS.

- [ ] **Step 7: `tsc` e commit**

```bash
bunx tsc --noEmit
git add src/components/receipt/ src/index.css
git commit -m "feat(front): receipt print view + window.print trigger"
```

---

## Task 4 (front): Ligar o botão de imprimir em `PedidosTab.tsx`

**Files:**
- Modify: `src/components/dashboard/PedidosTab.tsx:13` (import), `:418` (botão)

**Interfaces:**
- Consumes: `printReceipt`, `ReceiptData` (Task 3).
- Produces: nada novo (ponto de integração terminal).

- [ ] **Step 1: Import**

Em `src/components/dashboard/PedidosTab.tsx`, ao lado do import de `IconButton` (linha 13):

```typescript
import { printReceipt } from "@/components/receipt/printReceipt"
```

- [ ] **Step 2: Trocar o botão desabilitado por um handler real**

Substituir (linha 418):

```tsx
<IconButton icon={<Printer className="h-4 w-4" />} label={tPrinter("printSoon")} disabled />
```

por:

```tsx
<IconButton
  icon={<Printer className="h-4 w-4" />}
  label={tPrinter("printOrder")}
  onClick={() =>
    printReceipt({
      title: tPrinter("receiptTitleOrder", { id: order.id }),
      customerName: order.customerName?.trim() || tCommon("notInformed"),
      dateLabel: formatDateTime(order.updatedAt || order.openedAt || '', activeLocale),
      items: (order.items ?? []).map((item) => ({
        name: item.product?.name ?? tOrders("fallback.product"),
        quantity: item.quantity,
        unitPrice: item.unitPriceAtOrder ?? item.product?.price ?? 0,
      })),
      total: order.total,
      locale: activeLocale,
    })
  }
/>
```

`tCommon`, `tOrders`, `activeLocale`, `formatDateTime` já existem nesse arquivo (usados nas colunas vizinhas, ver linhas 74, 398, 412).

- [ ] **Step 3: `tsc` — vai falhar até a Task 6 adicionar as chaves `printOrder`/`receiptTitleOrder`**

```bash
bunx tsc --noEmit
```

Esperado nesta task: sem erro de tipo (as chaves de tradução não são checadas pelo `tsc`, só em runtime pelo `i18n:check` — isso roda limpo na Task 6). Se `tsc` reclamar de tipo em `order.items`/`item.product`, conferir contra `src/domain/models.ts:38-58` (`OrderItem`/`Order`).

- [ ] **Step 4: Commit**

```bash
git add src/components/dashboard/PedidosTab.tsx
git commit -m "feat(front): wire print button in orders table"
```

---

## Task 5 (front): Ligar o botão de imprimir em `VendasTab.tsx`

**Files:**
- Modify: `src/components/dashboard/VendasTab.tsx:14` (import), `:405` (botão)

**Interfaces:**
- Consumes: `printReceipt`, `ReceiptData` (Task 3).
- Produces: nada novo (ponto de integração terminal).

- [ ] **Step 1: Import**

Em `src/components/dashboard/VendasTab.tsx`, ao lado do import de `IconButton`:

```typescript
import { printReceipt } from "@/components/receipt/printReceipt"
```

- [ ] **Step 2: Trocar o botão desabilitado por um handler real**

Substituir (linha 405):

```tsx
<IconButton icon={<Printer className="h-4 w-4" />} label={tPrinter("printSoon")} disabled />
```

por:

```tsx
<IconButton
  icon={<Printer className="h-4 w-4" />}
  label={tPrinter("printSale")}
  onClick={() =>
    printReceipt({
      title: tPrinter("receiptTitleSale", { id: sale.id }),
      customerName: sale.customerName?.trim() || tCommon("notInformed"),
      dateLabel: sale.soldAt ? formatDateTime(sale.soldAt, activeLocale) : tCommon("notInformed"),
      items: (sale.items ?? []).map((item) => ({
        name: item.product?.name ?? tSales("fallback.product"),
        quantity: item.quantity,
        unitPrice: item.unitPriceAtSale ?? item.product?.price ?? 0,
      })),
      total: sale.total,
      locale: activeLocale,
    })
  }
/>
```

`tCommon`, `tSales`, `activeLocale`, `formatDateTime` já existem nesse arquivo (linhas 73, 391-399).

- [ ] **Step 3: `tsc` e commit**

```bash
bunx tsc --noEmit
git add src/components/dashboard/VendasTab.tsx
git commit -m "feat(front): wire print button in sales table"
```

---

## Task 6 (front): Chaves i18n novas + seção "Impressão" em `SettingsPage.tsx`

**Files:**
- Modify: `src/i18n/locales/en.json`, `es.json`, `fr.json`, `hi.json`, `pt-BR.json`, `zh.json` (namespaces `printer` e `settings`)
- Modify: `src/pages/dashboard/SettingsPage.tsx`
- Modify: `src/pages/dashboard/SettingsPage.test.tsx`

**Interfaces:**
- Consumes: `PAPER_WIDTH_PRESETS`, `getStoredPaperWidth`, `persistPaperWidth` (Task 2); `Select`/`SelectTrigger`/`SelectContent`/`SelectItem`/`SelectValue` de `@/components/ui/select` (já existe, usado pra idioma).
- Produces: nada novo (tela terminal).

- [ ] **Step 1: Adicionar chaves novas nos 6 locale files**

Em cada `src/i18n/locales/<locale>.json`, dentro do objeto `"printer"` já existente, adicionar 3 chaves (mantendo as que já existem: `print`, `printOrder`, `printSale`, `printing`, `printSuccess`, `printError`, `printSoon`):

```json
"receiptTitleOrder": "Order #{{id}}",
"receiptTitleSale": "Sale #{{id}}",
"receiptTotal": "Total",
```

E dentro do objeto `"settings"` já existente (que já tem `title`, `appearance`, `theme`, `light`, `dark`, `system`, `language`, `currentLanguage`, `localeIndicator`, `locales`, `moreComingSoon`), adicionar 2 chaves:

```json
"printing": "Printing",
"paperWidth": "Paper width",
```

Valores por locale (mesmo texto em `printer.receiptTotal` e `settings.printing`/`paperWidth` — tradução literal do conceito, não muda por contexto):

| Chave | en | pt-BR | es | fr | zh | hi |
|---|---|---|---|---|---|---|
| `printer.receiptTitleOrder` | `Order #{{id}}` | `Pedido #{{id}}` | `Pedido #{{id}}` | `Commande #{{id}}` | `订单 #{{id}}` | `ऑर्डर #{{id}}` |
| `printer.receiptTitleSale` | `Sale #{{id}}` | `Venda #{{id}}` | `Venta #{{id}}` | `Vente #{{id}}` | `销售 #{{id}}` | `बिक्री #{{id}}` |
| `printer.receiptTotal` | `Total` | `Total` | `Total` | `Total` | `总计` | `कुल` |
| `settings.printing` | `Printing` | `Impressão` | `Impresión` | `Impression` | `打印` | `प्रिंटिंग` |
| `settings.paperWidth` | `Paper width` | `Largura do papel` | `Ancho del papel` | `Largeur du papier` | `纸张宽度` | `कागज़ की चौड़ाई` |

- [ ] **Step 2: Rodar o checker de i18n**

```bash
bun run i18n:check
```

Esperado: `i18n bundles OK: 6 locales, 14 namespaces` (sem chave faltando/sobrando).

- [ ] **Step 3: Adicionar a seção "Impressão" em `SettingsPage.tsx`**

Import novo no topo (junto dos outros):

```typescript
import { Printer } from "lucide-react"
import {
  PAPER_WIDTH_PRESETS,
  getStoredPaperWidth,
  persistPaperWidth,
  type PaperWidthPreset,
} from "@/lib/printPreferences"
import { useState } from "react"
```

Dentro de `SettingsPage`, junto dos outros hooks (após `handleLocaleChange`):

```typescript
const [paperWidth, setPaperWidth] = useState<PaperWidthPreset>(() => getStoredPaperWidth())

const handlePaperWidthChange = (value: string) => {
  setPaperWidth(persistPaperWidth(value))
}
```

Nova seção, inserida entre o bloco "Idioma" (que termina no `</div>` antes de `moreComingSoon`) e o placeholder `moreComingSoon`:

```tsx
<div className="p-6 border rounded-lg bg-card space-y-3">
  <h2 className="text-xl font-semibold flex items-center gap-2">
    <Printer className="h-5 w-5" />
    {t('printing')}
  </h2>
  <div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
    <label htmlFor="paper-width-select" className="text-muted-foreground">
      {t('paperWidth')}
    </label>
    <Select value={paperWidth} onValueChange={handlePaperWidthChange}>
      <SelectTrigger id="paper-width-select" className="w-[180px]">
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        {PAPER_WIDTH_PRESETS.map((preset) => (
          <SelectItem key={preset} value={preset}>
            {preset === 'a4' ? 'A4' : preset}
          </SelectItem>
        ))}
      </SelectContent>
    </Select>
  </div>
</div>
```

- [ ] **Step 4: Testes — adicionar cobertura em `SettingsPage.test.tsx`**

Adicionar ao final do `describe('SettingsPage', ...)` existente:

O `beforeEach` do arquivo já troca pro `pt-BR` (`await i18n.changeLanguage('pt-BR')`) antes de cada teste — os testes novos usam o texto em pt-BR (`Largura do papel`, `settings.paperWidth`), mesmo padrão dos testes de idioma já existentes no arquivo (`'Idioma atual'`):

```typescript
it('shows the printing section with the persisted paper width', () => {
  localStorage.setItem('tozzo.printerWidth', '58mm')
  renderWithProviders()
  expect(screen.getByRole('combobox', { name: 'Largura do papel' })).toHaveTextContent('58mm')
})

it('changes and persists the selected paper width', async () => {
  const user = userEvent.setup()
  renderWithProviders()

  await user.click(screen.getByRole('combobox', { name: 'Largura do papel' }))
  await user.click(await screen.findByRole('option', { name: '110mm' }))

  expect(localStorage.getItem('tozzo.printerWidth')).toBe('110mm')
})
```

- [ ] **Step 5: Rodar os testes**

```bash
bun test src/pages/dashboard/SettingsPage.test.tsx
```

Esperado: todos passam.

- [ ] **Step 6: `tsc`, build e checker final**

```bash
bunx tsc --noEmit
bun run build
bun run i18n:check
```

- [ ] **Step 7: Commit**

```bash
git add src/i18n/locales/ src/pages/dashboard/SettingsPage.tsx src/pages/dashboard/SettingsPage.test.tsx
git commit -m "feat(front): paper-width setting + i18n keys for print"
```

---

## Task 7 (mobile): `constants/printerWidths.ts` + `services/printerPreferences.ts`

**Files:**
- Create: `constants/printerWidths.ts`
- Create: `services/printerPreferences.ts`
- Test: `services/__tests__/printerPreferences.test.ts`

**Interfaces:**
- Consumes: `AsyncStorage` de `@react-native-async-storage/async-storage`.
- Produces: `PRINTER_WIDTH_PRESETS` (array), `PrinterWidthPreset` (union: `'44mm' | '58mm' | '76mm' | '80mm' | '110mm'`), `PRINTER_WIDTH_COLUMNS` (`Record<PrinterWidthPreset, number>`), `DEFAULT_PRINTER_WIDTH` (`'80mm'`) de `constants/printerWidths.ts`; `getPrinterWidth(): Promise<PrinterWidthPreset>`, `setPrinterWidth(value: unknown): Promise<PrinterWidthPreset>`, `PRINTER_WIDTH_PREFERENCE_KEY` de `services/printerPreferences.ts`. Tasks 8 e 9 consomem ambos.

- [ ] **Step 1: Tabela de larguras (sem teste — é uma constante pura, mesmo padrão de `constants/status.ts`)**

```typescript
// constants/printerWidths.ts
export const PRINTER_WIDTH_PRESETS = ['44mm', '58mm', '76mm', '80mm', '110mm'] as const;

export type PrinterWidthPreset = (typeof PRINTER_WIDTH_PRESETS)[number];

export const DEFAULT_PRINTER_WIDTH: PrinterWidthPreset = '80mm';

// Colunas por linha na fonte A (ESC/POS, 203dpi) — aproximação padrão de
// mercado. Sem impressora física de 44/76/110mm pra calibrar; ajuste fino
// futuro é mudar 1 número aqui, não redesenho.
export const PRINTER_WIDTH_COLUMNS: Record<PrinterWidthPreset, number> = {
  '44mm': 24,
  '58mm': 32,
  '76mm': 42,
  '80mm': 48,
  '110mm': 64,
};
```

- [ ] **Step 2: Escrever o teste da persistência (mesmo padrão de `i18n/__tests__/index.test.ts` pra locale)**

```typescript
// services/__tests__/printerPreferences.test.ts
import AsyncStorage from '@react-native-async-storage/async-storage';
import { getPrinterWidth, setPrinterWidth, PRINTER_WIDTH_PREFERENCE_KEY } from '../printerPreferences';

jest.mock('@react-native-async-storage/async-storage', () => ({
  __esModule: true,
  default: {
    getItem: jest.fn(async () => null),
    setItem: jest.fn(async () => undefined),
  },
}));

const mockGetItem = AsyncStorage.getItem as jest.Mock;
const mockSetItem = AsyncStorage.setItem as jest.Mock;

describe('printerPreferences', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('falls back to 80mm when nothing is stored', async () => {
    mockGetItem.mockResolvedValueOnce(null);
    expect(await getPrinterWidth()).toBe('80mm');
  });

  it('returns a valid stored preset', async () => {
    mockGetItem.mockResolvedValueOnce('58mm');
    expect(await getPrinterWidth()).toBe('58mm');
  });

  it('falls back to 80mm for a corrupted value', async () => {
    mockGetItem.mockResolvedValueOnce('bogus');
    expect(await getPrinterWidth()).toBe('80mm');
  });

  it('falls back to 80mm when storage throws', async () => {
    mockGetItem.mockRejectedValueOnce(new Error('storage unavailable'));
    expect(await getPrinterWidth()).toBe('80mm');
  });

  it('persists a valid preset', async () => {
    const result = await setPrinterWidth('110mm');
    expect(result).toBe('110mm');
    expect(mockSetItem).toHaveBeenCalledWith(PRINTER_WIDTH_PREFERENCE_KEY, '110mm');
  });

  it('persisting an invalid value falls back to 80mm', async () => {
    const result = await setPrinterWidth('bogus');
    expect(result).toBe('80mm');
    expect(mockSetItem).toHaveBeenCalledWith(PRINTER_WIDTH_PREFERENCE_KEY, '80mm');
  });
});
```

- [ ] **Step 3: Rodar o teste e confirmar que falha**

```bash
npx jest services/__tests__/printerPreferences.test.ts --watchAll=false
```

Esperado: falha, `services/printerPreferences.ts` não existe.

- [ ] **Step 4: Implementar**

```typescript
// services/printerPreferences.ts
import AsyncStorage from '@react-native-async-storage/async-storage';
import {
  DEFAULT_PRINTER_WIDTH,
  PRINTER_WIDTH_PRESETS,
  PrinterWidthPreset,
} from '@/constants/printerWidths';

export const PRINTER_WIDTH_PREFERENCE_KEY = '@tozzoburger/printerWidth';

function isValidPreset(value: unknown): value is PrinterWidthPreset {
  return typeof value === 'string' && (PRINTER_WIDTH_PRESETS as readonly string[]).includes(value);
}

export async function getPrinterWidth(): Promise<PrinterWidthPreset> {
  try {
    const stored = await AsyncStorage.getItem(PRINTER_WIDTH_PREFERENCE_KEY);
    return isValidPreset(stored) ? stored : DEFAULT_PRINTER_WIDTH;
  } catch {
    return DEFAULT_PRINTER_WIDTH;
  }
}

export async function setPrinterWidth(value: unknown): Promise<PrinterWidthPreset> {
  const preset = isValidPreset(value) ? value : DEFAULT_PRINTER_WIDTH;
  await AsyncStorage.setItem(PRINTER_WIDTH_PREFERENCE_KEY, preset);
  return preset;
}
```

- [ ] **Step 5: Rodar o teste e confirmar que passa**

```bash
npx jest services/__tests__/printerPreferences.test.ts --watchAll=false
```

Esperado: 6/6 pass.

- [ ] **Step 6: `tsc` e commit**

```bash
npx tsc --noEmit
git add constants/printerWidths.ts services/printerPreferences.ts services/__tests__/printerPreferences.test.ts
git commit -m "feat(mobile): paper-width preference storage (AsyncStorage)"
```

---

## Task 8 (mobile): Generalizar `formatarVendaParaImpressao` pra usar a largura configurada

**Files:**
- Modify: `hooks/formatarVendaImpressao.ts`
- Test: `hooks/__tests__/formatarVendaImpressao.test.ts` (novo)
- Modify: `app/(tabs)/historico.tsx:367` (adicionar `await`)
- `app/modais/contaHistoricoModal.tsx` — nenhuma mudança (já usa `await` na chamada, ver Step 5)

**Interfaces:**
- Consumes: `getPrinterWidth`, `PRINTER_WIDTH_COLUMNS` (Task 7).
- Produces: `formatarVendaParaImpressao` passa a ser `async` — mesma assinatura de parâmetros `(venda: Sale, produtos: Produto[])`, retorno vira `Promise<string>`.

- [ ] **Step 1: Escrever o teste (não existe teste hoje pra esse arquivo)**

```typescript
// hooks/__tests__/formatarVendaImpressao.test.ts
import { formatarVendaParaImpressao } from '../formatarVendaImpressao';
import { getPrinterWidth } from '@/services/printerPreferences';
import { i18n } from '@/i18n';

jest.mock('@/services/printerPreferences', () => ({
  getPrinterWidth: jest.fn(async () => '80mm'),
}));

const mockGetPrinterWidth = getPrinterWidth as jest.Mock;

const baseSale = {
  id: 'venda-1',
  total: 20,
  soldAt: new Date('2026-08-26T10:00:00Z').toISOString(),
  customerName: 'Jane',
} as any;

describe('formatarVendaParaImpressao', () => {
  beforeAll(async () => {
    await i18n.changeLanguage('en');
  });

  beforeEach(() => {
    jest.clearAllMocks();
    mockGetPrinterWidth.mockResolvedValue('80mm');
  });

  it('produces a longer padded line at 80mm (48 cols) than at 44mm (24 cols) for the same item', async () => {
    const produtos = [{ name: 'Burger', quantity: 1, price: 10 }];

    mockGetPrinterWidth.mockResolvedValueOnce('80mm');
    const at80mm = await formatarVendaParaImpressao(baseSale, produtos);

    mockGetPrinterWidth.mockResolvedValueOnce('44mm');
    const at44mm = await formatarVendaParaImpressao(baseSale, produtos);

    const dotsLength = (text: string) => (text.match(/\.{2,}/)?.[0].length ?? 0);
    expect(dotsLength(at80mm)).toBeGreaterThan(dotsLength(at44mm));
  });

  it('never produces a negative dot count for a long product name at the narrowest width (44mm, 24 cols)', async () => {
    mockGetPrinterWidth.mockResolvedValueOnce('44mm');
    const produtos = [{ name: 'A'.repeat(40), quantity: 1, price: 999.99 }];
    const result = await formatarVendaParaImpressao(baseSale, produtos);
    expect(result).not.toContain('-');
  });

  it('resolves the customer name and total in the output', async () => {
    const result = await formatarVendaParaImpressao(baseSale, [{ name: 'Burger', quantity: 1, price: 10 }]);
    expect(result).toContain('Jane');
    expect(result).toContain('$20.00');
  });
});
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

```bash
npx jest hooks/__tests__/formatarVendaImpressao.test.ts --watchAll=false
```

Esperado: falha — a função ainda não é `async` e não usa `getPrinterWidth`.

- [ ] **Step 3: Implementar — trocar `48` fixo pela largura resolvida**

Em `hooks/formatarVendaImpressao.ts`, adicionar o import e tornar a função `async`:

```typescript
import { Sale } from "@/database/types/Sale";
import { i18n } from "@/i18n";
import { getPrinterWidth } from "@/services/printerPreferences";
import { PRINTER_WIDTH_COLUMNS } from "@/constants/printerWidths";

export interface Produto {
  name: string;
  quantity: number;
  price: number;
}

export async function formatarVendaParaImpressao(venda: Sale, produtos: Produto[]): Promise<string> {
  const columns = PRINTER_WIDTH_COLUMNS[await getPrinterWidth()];
  const locale = i18n.language;
  // ... (o corpo da função continua igual, só troca os dois usos literais de
  // `48` — hoje só existe em `numPontosLinha = 48 - (...)` — por `columns`)
```

A única linha que muda dentro do `forEach` de produtos é:

```typescript
const numPontosLinha = columns - (productName.length + totalPrice.length + 8);
```

(era `48 - (...)`; o resto do corpo da função — cabeçalho, formatação de moeda/data, loop de itens, total final — fica idêntico ao que já existe hoje.)

- [ ] **Step 4: Rodar o teste e confirmar que passa**

```bash
npx jest hooks/__tests__/formatarVendaImpressao.test.ts --watchAll=false
```

Esperado: 3/3 pass.

- [ ] **Step 5: Atualizar o único call site que ainda falta `await`**

`app/modais/contaHistoricoModal.tsx:136` já chama `await formatarVendaParaImpressao(venda, produtos)` (escrito assim antes mesmo da função virar `async` — `await` num valor não-Promise só resolve na hora, então já funcionava e continua funcionando sem mudança nenhuma aqui).

`app/(tabs)/historico.tsx:367` é o único que falta `await`. Trocar:

```typescript
await sendMessageToDevice(formatarVendaParaImpressao(sale, products), await getPrinter());
```

por:

```typescript
await sendMessageToDevice(await formatarVendaParaImpressao(sale, products), await getPrinter());
```

- [ ] **Step 6: Rodar a suíte completa do mobile e `tsc`**

```bash
npx jest --watchAll=false --runInBand
npx tsc --noEmit
```

Esperado: tudo verde (confirma que nenhum outro teste dependia da função ser síncrona).

- [ ] **Step 7: Commit**

```bash
git add hooks/formatarVendaImpressao.ts hooks/__tests__/formatarVendaImpressao.test.ts app/\(tabs\)/historico.tsx app/modais/contaHistoricoModal.tsx
git commit -m "feat(mobile): generalize receipt column width beyond the hardcoded 48"
```

---

## Task 9 (mobile): Seletor de largura em `app/(tabs)/configs.tsx` + i18n

**Files:**
- Modify: `i18n/locales/en/printer.json`, `es/printer.json`, `fr/printer.json`, `hi/printer.json`, `pt-BR/printer.json`, `zh/printer.json`
- Modify: `app/(tabs)/configs.tsx`
- Modify: `app/(tabs)/__tests__/configsI18n.test.tsx`

**Interfaces:**
- Consumes: `PRINTER_WIDTH_PRESETS`, `PrinterWidthPreset` (Task 7); `getPrinterWidth`, `setPrinterWidth` (Task 7).
- Produces: nada novo (tela terminal).

- [ ] **Step 1: Adicionar a chave `paperWidth` nos 6 `printer.json`**

Em cada `i18n/locales/<locale>/printer.json`, adicionar (mesmo valor usado no front pra `settings.paperWidth` — ver Task 6):

```json
"paperWidth": "Paper width",
```

| Locale | Valor |
|---|---|
| en | `Paper width` |
| pt-BR | `Largura do papel` |
| es | `Ancho del papel` |
| fr | `Largeur du papier` |
| zh | `纸张宽度` |
| hi | `कागज़ की चौड़ाई` |

- [ ] **Step 2: Rodar o checker de i18n do mobile**

```bash
npx jest --watchAll=false --testPathPattern check-i18n
```

Esperado: passa (6 locales, mesma contagem de chaves em `printer`).

- [ ] **Step 3: Adicionar estado + Picker em `configs.tsx`**

Import novo (junto do bloco de imports de `@/i18n`):

```typescript
import { PRINTER_WIDTH_PRESETS, PrinterWidthPreset } from '@/constants/printerWidths';
import { getPrinterWidth, setPrinterWidth } from '@/services/printerPreferences';
```

Dentro de `BluetoothScreen`, junto dos outros `useState`/`useEffect` de preferência (perto de `selectedLocale`):

```typescript
const [selectedPrinterWidth, setSelectedPrinterWidth] = useState<PrinterWidthPreset>('80mm');
const [printerWidthChanging, setPrinterWidthChanging] = useState(false);

useEffect(() => {
  getPrinterWidth().then(setSelectedPrinterWidth);
}, []);

const handlePrinterWidthChange = async (value: PrinterWidthPreset) => {
  setPrinterWidthChanging(true);
  try {
    const applied = await setPrinterWidth(value);
    setSelectedPrinterWidth(applied);
  } catch (error) {
    console.error('Failed to save printer width preference:', error);
  } finally {
    setPrinterWidthChanging(false);
  }
};
```

No JSX, dentro da seção "Printer" (`app/(tabs)/configs.tsx:224-244`), adicionar o Picker de largura logo abaixo do bloco `connectedPrinter ? (...) : (...)`, ainda dentro de `styles.sectionContent`:

```tsx
<View style={{ marginTop: 16 }}>
  <Text style={{ marginBottom: 8, color: colors.text }}>{t('printer.paperWidth')}</Text>
  <View style={[styles.pickerFrame, { borderColor: colors.border, backgroundColor: colors.surface }]}>
    <Picker
      selectedValue={selectedPrinterWidth}
      onValueChange={(value) => handlePrinterWidthChange(value as PrinterWidthPreset)}
      enabled={!printerWidthChanging}
      accessibilityLabel={t('printer.paperWidth')}
      style={{ color: colors.text }}
      dropdownIconColor={colors.text}
    >
      {PRINTER_WIDTH_PRESETS.map((preset) => (
        <Picker.Item key={preset} label={preset} value={preset} />
      ))}
    </Picker>
  </View>
</View>
```

- [ ] **Step 4: Corrigir o teste existente — agora há 2 Pickers na tela**

`app/(tabs)/__tests__/configsI18n.test.tsx` tem um teste que hoje faz `findAll((node) => node.type === 'PickerItem')` sem distinguir qual Picker — com o Picker de largura novo isso passa a achar 11 itens (6 locales + 5 larguras) em vez de 6, e o teste quebra. Trocar o filtro pra restringir aos valores de locale:

Substituir:

```typescript
it('offers exactly the six supported locales', () => {
  const renderer = create(
    <I18nextProvider i18n={i18n}>
      <BluetoothScreen />
    </I18nextProvider>,
  );

  const items = renderer.root.findAll((node) => (node.type as string) === 'PickerItem');
  expect(items.map((item) => item.props.value)).toEqual([...SUPPORTED_LOCALES]);
});
```

por:

```typescript
it('offers exactly the six supported locales', () => {
  const renderer = create(
    <I18nextProvider i18n={i18n}>
      <BluetoothScreen />
    </I18nextProvider>,
  );

  const items = renderer.root.findAll(
    (node) => (node.type as string) === 'PickerItem' && (SUPPORTED_LOCALES as readonly string[]).includes(node.props.value),
  );
  expect(items.map((item) => item.props.value)).toEqual([...SUPPORTED_LOCALES]);
});
```

- [ ] **Step 5: Adicionar o teste novo pro Picker de largura**

No mesmo arquivo, adicionar ao final do `describe`:

```typescript
it('offers exactly the five printer paper widths', () => {
  const renderer = create(
    <I18nextProvider i18n={i18n}>
      <BluetoothScreen />
    </I18nextProvider>,
  );

  const items = renderer.root.findAll(
    (node) => (node.type as string) === 'PickerItem' && ['44mm', '58mm', '76mm', '80mm', '110mm'].includes(node.props.value),
  );
  expect(items.map((item) => item.props.value)).toEqual(['44mm', '58mm', '76mm', '80mm', '110mm']);
});
```

- [ ] **Step 6: Rodar os testes**

```bash
npx jest app/\(tabs\)/__tests__/configsI18n.test.tsx --watchAll=false
```

Esperado: todos passam.

- [ ] **Step 7: Suíte completa + `tsc`**

```bash
npx jest --watchAll=false --runInBand
npx tsc --noEmit
```

- [ ] **Step 8: Commit**

```bash
git add i18n/locales/ "app/(tabs)/configs.tsx" "app/(tabs)/__tests__/configsI18n.test.tsx"
git commit -m "feat(mobile): paper-width picker in settings"
```
