# Revisão independente 4 — Task 3 (API sync)

Data: 2026-08-27  
Repositório: `C:\RN\api\api-tozzo.uk`  
Branch: `feat/fase-7-sync-status-categoria`  
Commit revisado: `6e07dcab88726a22ba15b8f05f8deac07f2be0bd`  

## Veredito final

# APROVADO

A Task 3 está aprovada considerando as quatro rodadas de correção. O achado I1-R1 da rodada 3 foi corrigido no commit `6e07dca`, sem regressão observada em C1, I2 ou I3.

## Escopo e histórico

Foram lidos:

- `2026-08-27-fase-7-t3-review3.md`, que registrou I1-R1 nas três consultas de itens dos gráficos;
- `2026-08-27-fase-7-t3-api-worker-fix2.md`, que descreveu o fix proposto;
- o diff completo de `6e07dca`.

Comando executado:

```text
git show 6e07dca --stat
```

Resultado relevante:

```text
commit 6e07dcab88726a22ba15b8f05f8deac07f2be0bd
    fix-charts-I1-R1-exclude-soft-deleted-sale-items

 modules/charts/charts.controller.ts    |   3 +
 tests/charts/charts.controller.test.ts | 122 ++++++++++++++++++++++++++++++++-
 2 files changed, 124 insertions(+), 1 deletion(-)
```

Também foi executado `git show --no-ext-diff --format=fuller --find-renames 6e07dca --` e o diff completo foi lido. A alteração de produção adiciona exatamente três predicados `deletedAt: null`; o restante é fixture e teste de regressão.

## Verificação de I1-R1

Comando:

```text
git grep -n -i saleItem -- modules/charts
```

Resultado:

```text
modules/charts/charts.controller.ts:39:    const itensVenda = await prisma.saleItem.findMany({
modules/charts/charts.controller.ts:142:    const itensVenda = await prisma.saleItem.findMany({
modules/charts/charts.controller.ts:264:        const itensVenda = await prisma.saleItem.findMany({
```

As três consultas, em `obterGraficoTopProdutos`, `obterListaDetalhada` e na geração assíncrona de relatório, têm no `where`:

```ts
deletedAt: null,
```

Não há consulta de `OrderItem`:

```text
git grep -n orderItem -- modules/charts
exit 1 (nenhuma ocorrência)
```

Também não há relação/consulta `items` no controller de gráficos. O inventário de operações Prisma no arquivo retorna somente as três consultas de `saleItem`, uma consulta de `establishment` para o título e uma consulta de `sale` para vendas por horário. Portanto, não ficou outra consulta de item sem o filtro.

## Teste de regressão

Comando executado por esta revisão:

```text
bun test --isolate --parallel tests/charts/charts.controller.test.ts --test-name-pattern=conta
```

Resultado real:

```text
1 pass
2 filtered out
0 fail
3 expect() calls
Ran 1 test across 1 file.
exit code 0
```

O teste não é vazio nem tautológico:

- a fixture cria uma venda ativa com dois itens distintos: item ativo com quantidade `2` e `deletedAt: null`, e item tombstonado com quantidade `5` e `deletedAt` preenchido;
- o fake `saleItem.findMany` aplica o `where.deletedAt`: quando o predicado é omitido, ambos passam; quando é `null`, somente o item ativo passa;
- a asserção exige exatamente o produto ativo, `2` unidades, faturamento `20`, fechamento com `2` unidades e `20` faturados, e não permite o produto tombstonado.

Evidência estática no teste:

```text
tests/charts/charts.controller.test.ts:27:  deletedAt: Date | null;
tests/charts/charts.controller.test.ts:65:        deletedAt: null,
tests/charts/charts.controller.test.ts:73:        deletedAt: new Date('2026-08-27T12:05:00.000Z'),
tests/charts/charts.controller.test.ts:90:      .filter((item) => where.deletedAt === undefined || item.deletedAt === where.deletedAt)
tests/charts/charts.controller.test.ts:49:    id: 'product-deleted',
tests/charts/charts.controller.test.ts:170:      totalUnidadesVendidas: 2,
```

Assim, retirar o `deletedAt: null` do controller faria o fake retornar os dois itens e a igualdade exata falharia.

## Regressão dos critérios C1, I2 e I3

O commit `6e07dca` não altera `modules/sync/sync.controller.ts`:

```text
git diff --name-only 6e07dca~1 6e07dca -- modules/sync/sync.controller.ts
nenhuma saída
```

A leitura do HEAD confirmou que as correções anteriores continuam presentes:

- C1: `conditionalWhere` inclui `updatedAt: { lte: cursorDate }`; `updateWithConflict` usa `updateMany` com esse predicado e lança `SyncConflictError` quando `count !== 1` (`modules/sync/sync.controller.ts:366-401`). A conversão de unicidade `P2002` continua presente.
- I2: `applyProductType` exige `UserRole.OWNER` e protege `INITIAL_PRODUCT_TYPES` em create/update (`:487-498`); o caminho de delete aplica as mesmas duas guardas (`:759-769`) e registra a alteração como ignorada.
- I3: após a transação, os eventos `pedidos` e `vendas` permanecem em blocos `try/catch` independentes (`:1002-1015`), preservando a resposta do push quando SSE falha.

Testes direcionados executados:

```text
bun test --isolate --parallel tests/sync/sync.postgres.concurrent.test.ts
1 pass, 0 fail, 6 expect() calls
exit code 0

bun test --isolate --parallel tests/sync/sync.protocol.test.ts --test-name-pattern=product
2 pass, 0 fail, 7 expect() calls
16 filtered out
exit code 0

bun test --isolate --parallel tests/sync/sync.protocol.test.ts --test-name-pattern=emits
1 pass, 0 fail, 5 expect() calls
17 filtered out
exit code 0
```

O teste de C1 usou Postgres real via Docker. Os logs esperados do teste de I3 mostram as falhas injetadas de SSE sendo registradas, mas o teste terminou com pass e zero falhas.

## Verificação completa

A primeira tentativa da suíte no sandbox terminou em `206 pass, 2 fail`; ambos os failures ocorreram antes da execução dos testes Postgres por `permission denied` no pipe `docker_engine`. A suíte foi então repetida com acesso ao Docker.

Comando final executado:

```text
bun test --isolate --parallel
```

Resultado final real:

```text
208 pass
0 fail
563 expect() calls
Ran 208 tests across 25 files.
exit code 0
```

TypeScript:

```text
bunx tsc --noEmit
exit code 0
sem saída
```

O `git diff --check 6e07dca~1 6e07dca` terminou com exit code 0 e sem erros.

## Identificadores e estado do repositório

A leitura do diff completo mostra que a produção só recebeu o identificador/campo já previsto `deletedAt`; os novos identificadores do teste (`FakeChartProduct`, `FakeChartSaleItem`, `chartSales`, `seedChartSale`, `activeProduct`, `deletedProduct`, entre outros) são em inglês. As ocorrências em português no teste são strings de descrição, não identificadores novos.

O snapshot final foi:

```text
git status --short --branch
## feat/fase-7-sync-status-categoria

git diff --name-only
nenhuma saída
```

Não houve alteração de código nem novo commit durante a revisão.

## Conclusão

O fix cobre as três consultas apontadas em I1-R1, o teste de regressão prova que um `SaleItem` tombstonado não é contado junto de um item ativo, a suíte completa está em `208 pass / 0 fail`, o TypeScript está limpo e C1/I2/I3 continuam corrigidos. Não há achado bloqueante novo.

**Veredito final: APROVADO.**
